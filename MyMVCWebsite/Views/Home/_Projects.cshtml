@model List<ProjectModel>

@foreach (var job in Model)
{
    <div class="row experience-wrap my-5">
        <div class="col-3">
            <a href="@job.URL" target="_blank"><img src="@job.ImageUrl" alt="@job.Title" class="img-thumbnail mb-3  hover-scale-lg" /></a>
            <span class="date-range">@job.ProjectDate</span>
        </div>
        <div class="col-9">
            <h4 class="text-emphasis hover-scale-lg"><a href="@job.URL" target="_blank">@job.Title</a></h4>
            <p>@job.Description</p>
            <ul class="skills-wrap">
                @foreach (var skill in job.Skills)
                {
                    <li class="skill">@skill.Name</li>
                }
            </ul>
        </div>
    </div>
}